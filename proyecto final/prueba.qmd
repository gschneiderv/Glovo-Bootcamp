Levanto el dataframe y le coloco como delimitador ";"

```{r}
library(tidyverse)
df<-read.csv(file= '/Users/gaby/Desktop/Repos/Glovo-Bootcamp/proyecto final/airbnb-listings.csv', sep = ';')
```

Averiguo cuántas columnas y filas tiene

```{r}
paste("El dataframe tiene",length(rownames(df)), " filas y", length(colnames(df)),"columnas." )
```

```{r}
summary(df)
```
